import json

it = json.loads(open('../quizs/it.json', 'r'))
business = json.loads(open('../quizs/business.json', 'r'))
graphics = json.loads(open('../quizs/graphics.json', 'r'))
language = json.loads(open('../quizs/language.json', 'r'))
